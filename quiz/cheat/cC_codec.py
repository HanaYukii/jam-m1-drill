# -*- coding: utf-8 -*-
SHEETS = [{
 "ch": "C",
 "title": "Codec（序列化）",
 "one": "E(·) 把所有型別編成 octet 序列，並且必須是雙射 —— 因為 state root 直接建立在編碼結果上，"
        "任何「兩種寫法都合法」的空間都是分叉風險。",
 "flow": [
   "整數：固定長度用 E_n（小端）；一般整數用變長編碼，長度由第一個位元組的前綴決定",
   "序列：長度前綴 + 逐項編碼；固定長度的序列不帶長度前綴",
   "字典：先依 key 排序再編碼 —— 排序是編碼的一部分，不是實作細節",
   "optional：0 表示 ∅、1 表示有值再接內容",
   "tuple：逐欄位串接，沒有分隔符，所以欄位順序就是規格",
   "位元序列打包成位元組時採 LSB-first（與附錄 D trie 節點判斷位元用的 MSB-first 相反）",
 ],
 "consts": [
   ["雙射（bijective）", "每個值只有一種編碼、每個合法編碼只對應一個值"],
   ["小端", "固定長度整數一律小端"],
   ["LSB-first", "位元序列 → 位元組的打包方向（§C）；別跟 §D 的 MSB-first 混"],
 ],
 "eqs": [
   ["§C.1", "E 的遞迴定義與各型別的規則"],
   ["§C.x 變長整數", "前綴長度編碼的分界值"],
   ["§D.2", "反例對照：state key 的位元判斷是 MSB-first"],
 ],
 "asked": [
   ["為什麼編碼一定要是雙射？",
    "state root 是對編碼結果做 hash。如果同一個狀態能編出兩種位元組，兩個誠實節點就會算出不同的 root；"
    "反過來，如果兩個不同狀態能編出同一串位元組，攻擊者就能拿一份證明去證另一件事。"
    "雙射同時堵住這兩個方向。"],
   ["字典為什麼要排序後編碼？",
    "字典本身沒有順序，但編碼必須是確定的。以 key 排序是最簡單的正規化，"
    "任何實作（不論內部用 hash map 還是 tree）都能得到同一串位元組。"
    "實作上最常見的 bug 就是直接照 map 的迭代順序編碼。"],
   ["變長整數編碼為什麼不用固定 8 bytes 就好？",
    "狀態裡絕大多數整數都很小（索引、長度、計數）。固定 8 bytes 會讓狀態與證明肥好幾倍，"
    "而 Merkle 證明的大小是直接的頻寬成本。變長編碼在小數值上省很多，代價是要小心「同一個值只有一種寫法」。"],
   ["LSB-first 和 MSB-first 各用在哪？",
    "附錄 C 的位元序列打包成位元組是 LSB-first（例如 assurance 的 bitfield）；"
    "附錄 D 走 state trie 時把 key 展開成位元流是 MSB-first。"
    "兩者方向相反是刻意的，也是實作最容易寫反的地方之一。"],
 ],
 "delta": [
   "0.8.0 的編碼規則本身變動不大，但被編碼的結構（ρ、β item、refinement context）變了，序列化要跟著改",
 ],
 "code": [
   "encode.go 的每個 Encode 都應該有對應的 Decode 並做 round-trip 測試，這是抓非雙射的最快方法",
   "bitfield 的打包方向錯了，assurance 全部會驗不過，但單元測試如果自己編自己解會看不出來",
 ],
}]
